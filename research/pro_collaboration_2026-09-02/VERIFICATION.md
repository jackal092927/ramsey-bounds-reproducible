# Verification at consolidation

Date: 2026-09-03 PDT. These checks support the recorded computations and artifact consistency. They do not certify novelty, the full gadget reduction, or an end-to-end quantum advantage.

| Check | Result | Recorded evidence |
|---|---|---|
| Seven existing direction-selection probes | All passed | [Initial replay with source hashes and output](verification/existing_probes.json). |
| Independent persistent-boundary probe | 39 pair-degree checks across six families passed | [Full result](verification/persistent_unweighting.json); [implementation](check_persistent_unweighting.py). |
| Quantum Ramsey exact self-check | QUANTUM_RAMSEY_AUDIT_PASS | [Synchronization checks](verification/repository_sync_checks.json). |
| Frozen repository integrity | 13 files verified | Same synchronization record. |
| Equation-reference materialization regressions | Both passed | Same synchronization record. |
| Actual quantum sections and appendices in the unified manuscript | All ten match deterministic regeneration into a temporary directory | Same synchronization record, including canonical and generated hashes. |
| Current and submitted PDFs | All four hashes agree with the recorded artifacts; current quantum PDF equals submitted v2 | Same synchronization record. |

The independent persistent probe includes common-copy naturality, the scaled persistent operator, reducing asymmetric sectors, the asymmetric lower bound, and the ordinary-to-persistent domination inequality. It uses the kernel of outside boundary rows as the persistent domain and an orthonormal domain basis. The maximum intertwining residual is 1.6458982417795104e-15 against tolerance 2e-8. The two-triangle example detects the incorrect unrestricted row-deletion operator with residual 2.0. One nonflag family is explicitly outside the formal clique-complex theorem; it is retained as a diagnostic.

The earlier seven-probe replay preserves its original hashes. Only the hyperbolic probe's hash changed during the concurrent documentation cleanup, which removed machine-specific execution instructions; it was rerun against the current source and passed. No historical output was silently relabeled as a run of a different file.

Canonical quantum source and unified copies are intentionally different files: the generator adds a banner, namespaces labels and references, and changes the introductory section title and scoped wording. The consistency check regenerates into a temporary directory, then compares those expected generated bytes with the current unified copies. It does not overwrite the manuscript.

## Reproduce the new mathematical diagnostic

From the repository root, using the isolated dependency environment employed here:

```sh
uv run --no-project --python .venv/bin/python \
  --with numpy==2.4.4 --with scipy==1.17.1 \
  python research/pro_collaboration_2026-09-02/check_persistent_unweighting.py
```

The recorded interpreter was Python 3.11.15, with NumPy 2.4.4 and SciPy 1.17.1. These extra numerical dependencies were supplied in an isolated environment; no change to the repository's base dependency specification was made.

The existing quantum and repository checks use the commands shown verbatim in repository_sync_checks.json. Full historical DRAT/interval campaigns and LaTeX builds were not rerun in this consolidation. The manuscript build, historical certificate and submission statuses are attributed to their existing records; local hashes and generated-source consistency were freshly checked. No conference-server operation was performed.
