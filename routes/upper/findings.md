# Independent result-to-claim finding

## Exact-diagonal next-round retained-spine promotion

Date: 2026-08-12

```text
claim_supported = yes
confidence = high (within the stated pinned conditional/local trust boundary)
```

The exact ratio/separator proof, exhaustive two-dimensional sign reduction,
exact-diagonal compact enclosure, analytic tail, and complete retained-spine
wedge support

$$
\mathcal G_2^{(3)}\left(
\frac{330867}{500000},
\frac{12366348252219}{1250000000000}
\right)
$$

and, conditional on the pinned Yang--Mao v1 interfaces and frozen local
$P_6$/BookCor retained-spine chain,

$$
R(k,k)\le(3.780685290)^{k+o(k)}.
$$

The certified unrounded upper base is
`3.7806852883796401133524...`, in particular strictly below
`3.780685288379640114`.  Both author checkers, the genuinely non-importing
replay, the independent adaptive replay, and the resolved adversarial referee
pass at 512 bits.  No further local evidence is required for this scoped
conditional asymptotic claim.  The safe-base `3.780685300` result is the
immediate predecessor.

This evidence does not support a finite-$k$ threshold, a specific finite
Ramsey-number result, an unconditional theorem, global parameter optimality,
novelty, priority, publication, external peer review, or an unqualified
world-best claim.  The structured judgment, exact trust boundary, and frozen
hashes are in `../RESULT_TO_CLAIM_2026-08-12.md` and `PROGRESS.md`.

## $u_0=309/125$ diagonal-growth retained-spine promotion

Date: 2026-08-12

```text
claim_supported = yes
confidence = high (within the stated pinned conditional/local trust boundary)
```

The exact ratio/separator argument and the complete-sign-pattern reduction to
a certified diagonal-growth envelope support

$$
\mathcal G_2^{(3)}
\left(\frac{833}{1250},\frac{3092197917}{312500000}\right)
$$

and, conditional on the pinned Yang--Mao v1 interfaces and the frozen local
$P_6$/BookCor retained-spine chain,

$$
R(k,k)\le(3.780685300)^{k+o(k)}.
$$

The actual outward-rounded upper-base evaluation is
`3.7806852985874904057...`.  Both 512-bit author checks, a genuinely
non-importing 512-bit replay, and an independent mathematical referee pass.
No further local evidence is required for this scoped conditional asymptotic
claim.  This does not support a finite-$k$ threshold, a specific finite Ramsey
number, an unconditional theorem, global parameter optimality, novelty,
priority, publication, or an unqualified world-best claim.  The structured
judgment and complete hashes are in `../RESULT_TO_CLAIM_2026-08-12.md` and
`PROGRESS.md`.

## Specialized-correlation retained-spine promotion

Date: 2026-08-12

```text
claim_supported = yes
confidence = high (within the stated conditional/local trust boundary)
```

After resolution of two local exposition omissions, the 384-bit author
checks and separately structured 512-bit referee replay support

$$
\mathcal G_2^{(3)}(10^{-6},11.62088)
$$

and, conditional on Yang--Mao v1 plus the frozen local $P_6$/BookCor chain,

$$
R(k,k)\le(3.780687577208)^{k+o(k)}.
$$

This does not support an explicit finite-$k$ threshold, formal verification,
global parameter optimality, or a world-best/priority claim without a fresh
literature audit.  The structured judgment is in
`SHARPENED_RESULT_TO_CLAIM.md`; the proof replay and resolved hashes are in
`INDEPENDENT_RETAINED_SPINE_SHARPENED_REFEREE.md`.

Date: 2026-08-12  
Reviewer role: independent secondary proof replay, followed by adversarial
review of the strengthened split-`0.005` certificate

## Retained-spine numerical promotion

```text
claim_supported = yes
confidence = high (within the stated conditional/local trust boundary)
```

The independently refereed retained-spine transfer and its separately
replayed two-region Arb enclosure support

$$
R(k,k)\leq(3.780698389989140)^{k+o(k)}.
$$

This strictly improves the frozen P6 base
`3.7806984277961236987...`.  The exact certified benchmark is
`exp(U_1-1e-8)`, approximately `3.7806983899891396098`; it is an upper bound
for the retained-spine optimizer, not a claimed equality with that optimizer.
The result remains conditional on Yang--Mao v1, the frozen local P6 and
combinatorial proofs, and Arb containment.  It does not provide an explicit
finite-`k` threshold, external peer review/publication, or a global novelty
claim.  The complete structured judgment and the lower/finite claim
boundaries are in `../RESULT_TO_CLAIM_2026-08-12.md`.

## Verdict

```text
claim_supported = yes
confidence = high
```

The original independent review below remains the baseline review. The new
six-stage exact-linked result has passed the same proof gates plus the
generic-degree concavity audit and an independent adversarial replay:

```text
claim_supported = yes
confidence = high
```

Current supported claim:

$$
R(k,k)\le
(3.7806984277961236988\ldots)^{k+o(k)}.
$$

The six certificate hashes, exact coefficient links, margins, scope and
non-claims are frozen in `HIGHER_ORDER_RESULT_TO_CLAIM.md`,
`VERIFICATION_LOG_HIGHER_ORDER.md`, `STAGE4_SEARCH.md`, `STAGE5_SEARCH.md`, and
`STAGE6_SEARCH.md`.  The canonical six-stage chain and independently structured direct Arb replay
both pass.

## Earlier cubic baseline review

Supported baseline claim:

$$
R(k,k)\le
(3.7990629773286618741\ldots)^{k+o(k)}.
$$

The formerly imported GNNW Lemma 11/Theorem 12 combinatorial core now has a
standalone proof in `LEMMA11_STANDALONE_PROOF.md` and an independent adversarial
referee PASS in `INDEPENDENT_LEMMA11_REFEREE.md`.  Together with the repaired
BookCor proof in `BOOKCOR_AUDIT.md`, the candidate-specific finite-net theorem
in `INDEPENDENT_PROOF_REPLAY.md`, and Arb's documented ball semantics, this
closes the local implication.  Both the canonical verifier and an independently
structured direct-exponent verifier accept the frozen certificate
`certificate-recommended-beta00299-split005.json`.

It does not support the literal printed generality of GNNW Theorem 13, the
withdrawn `3.792918524...` route, the fragile beta-`0.0298` numerical frontier,
or any explicit finite-$k$ threshold.  Full details and margins are in
`RESULT_TO_CLAIM.md` and `VERIFICATION_LOG_SPLIT005.md`.
