# Ramsey research ledger

Last updated: 2026-08-12.

## Live claims

| Route | Claim or target | Evidence | Current status |
|---|---|---|---|
| Upper | GNNW numerical base `3.7992027396...` | arXiv v1 proof chain has an audited Lemma 14 gap | `PREPRINT-CLAIM / AUDIT GAP` |
| Upper | exact-diagonal retained-spine/P6, safe base `3.780685290` (actual `<3.780685288379640114`) | proves $\mathcal G_2^{(3)}(330867/500000,12366348252219/1250000000000)$; exponent gain `3.4754e-6`; two 512-bit author checks; genuinely non-importing and independent adaptive 512-bit reconstructions; resolved adversarial referee and secondary result-to-claim `yes/high`; complete hashes pinned in `routes/upper/PROGRESS.md` | `INDEPENDENTLY REVIEWED LOCAL COMPUTER-ASSISTED THEOREM`; conditional on pinned Yang--Mao v1 interfaces, frozen P6/BookCor and retained-spine transfer, and Arb; no finite-`k`, specific finite Ramsey, global optimum, novelty, priority, publication, or world-best claim |
| Upper | $u_0=309/125$ diagonal-growth retained-spine/P6, safe base `3.780685300` (actual `3.7806852985874904057...`) | proves $\mathcal G_2^{(3)}(833/1250,3092197917/312500000)$; exponent gain `3.4727e-6`; two 512-bit author checks; genuinely non-importing 512-bit reconstruction; independent referee and result-to-claim PASS | `INDEPENDENTLY REVIEWED IMMEDIATE CANONICAL PREDECESSOR` |
| Upper | strong-separator retained-spine/P6, safe base `3.780685405` (actual `3.7806854033124746271...`) | proves $\mathcal G_2^{(3)}(11/250,62025657/6250000)$; exponent gain `3.445e-6`; 384-bit author checks; non-importing 512-bit reconstruction; independent referee PASS | `INDEPENDENTLY REVIEWED IMMEDIATE CANONICAL PREDECESSOR` |
| Upper | diagonal-envelope intermediate, safe base `3.780685320` (actual `3.7806853125760260364...`) | two 512-bit author checks, non-importing 512-bit reconstruction, and independent referee PASS | `INDEPENDENTLY REVIEWED NONCANONICAL INTERMEDIATE` |
| Upper | hybrid-correlation retained-spine/P6, safe base `3.780685745` (actual `3.7806857435741762369...`) | proves $\mathcal G_2^{(3)}(1/4000000,9.9247984)$; exponent gain `3.355e-6`; 384-bit author checks; non-importing 512-bit reconstruction; independent referee/result-to-claim PASS | `INDEPENDENTLY REVIEWED LOCAL PREDECESSOR` |
| Upper | specialized-correlation retained-spine/P6, safe base `3.780687577208` | exponent gain `2.87e-6`; independent 512-bit reconstruction and resolved referee | `INDEPENDENTLY REVIEWED LOCAL PREDECESSOR` |
| Upper | optimized retained-spine/P6 hybrid, safe base `3.780695781309` | refereed analytic transfer; exponent gain `7e-7`; 384/448-bit replays plus adversarial 512-bit different-evaluator reconstruction; result-to-claim PASS | `INDEPENDENTLY REVIEWED LOCAL PREDECESSOR` |
| Upper | earlier retained-spine/P6 parameters, safe base `3.780698389989140` | exponent gain `1e-8`; independent 256-bit and 320-bit Arb enclosures | `INDEPENDENTLY REVIEWED LOCAL PREDECESSOR` |
| Upper | `3.7806984277961236988...`, exact-linked quintic/quintic/sextic/octic/decic/tetradecic descent | first three stages use 65,536-cell 256-bit Arb certificates; stages four through six use 131,072 cells; exact links; direct-slack replay; standalone combinatorial proof; independent adversarial referee | `INDEPENDENTLY REVIEWED FROZEN RATE INPUT` to the retained-spine theorem |
| Upper | `3.7807566104095593763...`, five-stage predecessor | exact-linked quintic/quintic/sextic/octic/decic chain and independent stage-five replay | `INDEPENDENTLY REVIEWED LOCAL PREDECESSOR` |
| Upper | `3.7814656158401685107...`, exact-linked three-stage predecessor | three 65,536-cell 256-bit Arb certificates; independent stage-three referee and full-chain adversarial review | `INDEPENDENTLY REVIEWED LOCAL PREDECESSOR` |
| Upper | `3.7990629773286618741...` with target `beta=.0299` | split-.005, 32,768-cell, 256-bit Arb certificate; independent direct-slack replay; two proof reviews | `COMPUTER-ASSISTED THEOREM`, external human review pending |
| Upper | target `beta=.03`, base `3.799202739615937...` | same safe-prior checker, robust positive control | `ARB-CERTIFIED REPAIR` |
| Upper | target `beta=.0298`, base `3.79892322018...` | Arb passes but worst main margin only `4.23e-9` | `NUMERICAL FRONTIER`, not recommended |
| Upper | Horizon `3.69608`, local delta `3.69588`, former `3.79292` | first two use orientation union; third assumes unproved `.03` prior | `WITHDRAWN` as theorem-linked candidates |
| Lower | `R(3,k)>=(1/2+o(1))k^2/log k` | Hefty--Horn--King--Pfender v3 | `PREPRINT-THEOREM` |
| Lower | fixed `s>=3`: `r(s,k)=Omega_s(k^(s-1)/(log k)^(2s-4))` | Bradač v3 | `PREPRINT-THEOREM` |
| Lower | fixed large `C`: public checkpoint `-1/2 log p_C + 1/20 + c_R(C)/(4D^2)` | exact red Appendix-B bridge, quantified extraction, unchanged blue ledger; `c_R/(4D^2)~1/(32 log C)` | `LOCAL PROOF-PACKAGE THEOREM`; external review pending |
| Lower | fixed sufficiently large $C$: controlled-residual extension $G_*(C)+\widehat H_*(C)$ | residual-gradient square completion, exact $K_r$ multiplicities, full-box concavity, three-factor centered costs, non-exchangeable reverse induction, and extraction; $\widehat H_*(C)\sim1/(64\log C)>0$ supersedes $H_*(C)\sim1/(96\log C)$; resolved source referee and secondary result-to-claim `yes/high` | `INDEPENDENTLY REVIEWED SOURCE-RELATIVE LOCAL PROOF-PACKAGE THEOREM`; existential/non-effective $C_0$; no all-$C$, finite Ramsey, new-blue, unconditional, novelty, priority, publication, world-best, global-optimality, adaptive-weight, or higher-order-potential claim |
| Lower | optimized fixed-large-`C` history-uniform companion `G_*(C)>c_R(C)/(4D^2)` | exact upper-truncated CGF, step-dependent maximal legal Holder split, limiting cutoff window; `G_*(C)~1/(32 log C)`; source-level referee PASS | `INDEPENDENTLY REVIEWED LOCAL PREDECESSOR`; `1/32` optimality only for deterministic history-uniform additive centered-moment deficits |
| Lower | paired HMS/refined truncated-Gaussian saving | common-history induction against both a frozen main-body ledger and the explicit red optimized appendix | `DERIVED LOCALLY`; blue companion refinement blocked but unnecessary for the large-`C` checkpoint |
| Lower | Bradač transverse-record proposal | a counted `Theta_t(q log q)` family has every step unmarked with `r=ell=0` and branching base at least `(3/16)q^(t+.9(t-1))`; independent replay passed | `UNIFORM RECORD MAP REFUTED`; no new lower bound |
| Lower | Bradač late-tail/weighted repair | state-saturated histories have `O_t(q^t)` fanout; absolute-depth tails and recoverable positive scalar contraction are obstructed; independent source replay passed | `PARTIAL POSITIVE LEMMA + MECHANISM OBSTRUCTION`; actual `q log q` tuple threshold remains open |
| Lower | Bradač cross-type transfer | exact flow $U_j'=U_j\setminus R_j$; root disproves universal multi-layer contraction; marked children coexist with unsaturated $U_0$; full $W(y)$ state is Markov | `INDEPENDENTLY REVIEWED MECHANISM OBSTRUCTION`; eventual diversification at a larger $A_t q\log q$ remains open |
| Lower | full-state symmetry quotient | projective-orthogonal full-state orbits are an exact strong lumping; positive supersolutions may be symmetrized; self-incidence profile gives fanout but is explicitly non-lumpable | `INDEPENDENTLY REVIEWED EXACT REDUCTION`; 23,962 states become 4,243 orbits for `t=2,q=2`; no new tuple or Ramsey bound |
| Finite | `R(3,17)>=93` | 92-vertex matrix, exact `omega=2`, `alpha=16`, SHA-256 `afbc45c0...bd35` | `INDEPENDENT-CERT` |
| Finite | `R(4,15)>=160` | 159-vertex matrix, exact `omega=3`, `alpha=14`, SHA-256 `05bf6700...dde1f` | `INDEPENDENT-CERT` |
| Finite | `R(3,13)>=62` search | frozen extension UNSAT; exact ten-triangle near miss; budget-nine hub forced; bounded solve unknown | `NO NEW BOUND` |
| Finite | non-star `k=11` fixed-seed repair basin | three hub-deleted branches plus one exact merged 512-transversal hub-retained branch; arbitrary additions plus at most ten old-edge deletions are UNSAT; four checked DRAT traces and independent reconstruction | `INDEPENDENTLY REVIEWED PROOF-CARRYING LOCAL EXCLUSION`; any completion needs at least eleven deletions; no new bound |
| Finite | second dispersed `R(3,13)` basin | 61 vertices, 369 edges, 17 triangles, no `I13`, maximum triangle-edge multiplicity 4, triangle transversal number 8; independent bitset/SAT review | `INDEPENDENTLY REVIEWED NEAR MISS`; `10<=m_cap4<=17`, exact optimum unknown; no new bound |
| Finite | `R(3,18)` one-point near miss | checked 99-point certificate for `R(3,18)>=100`; budget-at-most-five exclusion plus complete checked DRAT UNSAT proofs for all three exact-six triangle-edge branches; branch 0's 429,892-clause proof received author and independent fresh `s VERIFIED` replays | `INDEPENDENTLY REVIEWED PROOF-CARRYING LOCAL EXCLUSION`; this fixed seed needs at least seven input-edge deletions when original nonedges may be added freely; no exact-seven existence/nonexistence and no `R(3,18)>=101` claim |

## Upper-bound audit boundary

Established inside this workspace:

- the theorem's displayed `F'<0` sign is incompatible with its formula; the
  proof uses `F'>0`;
- a one-sided rate must satisfy both ordered-pair inequalities before it yields
  a point in the symmetric Ramsey region;
- Horizon's `min(bu,bs)` accepts a strict false positive for that rate
  certificate;
- GNNW Lemma 14 line 460 reverses a comparison, and its first positive-`alpha`
  numerical update fails under the envelope actually certified by the premise;
- printed Theorem 13 assumes `R` but invokes BookCor with an `R_*` input after
  shrinking only one coordinate; the current candidate has a separate
  strict-margin/elementary-region repair, but the general printed statement
  needs an added interior argument;
- its finite-net perturbation also needs `M` uniformly below one; the printed
  theorem omits this, while the current certificate has `M<0.393`;
- the exact-linked `.08 -> quintic -> quintic -> sextic -> octic -> decic -> tetradecic` chain passes
  the canonical Arb verifier and an independently structured direct-slack
  replay; stage six uses 131,072 cells, has standard direct-region margin
  `1.06281e-6`, main-inequality margin `4.81847e-5`, and base
  `3.7806984277961236988...`;
- an independent proof replay and an adversarial follow-up both accept the
  repaired finite-net implication, while a source audit repairs the local
  BookCor typos;
- `LEMMA11_STANDALONE_PROOF.md` now proves the formerly imported GNNW Lemma 11
  and Theorem 12 combinatorial core, including the corrected discrete-convexity
  book extraction and branch signs; an independent adversarial referee accepts
  the proof, so this is no longer an imported combinatorial dependency;
- independent fourth-, fifth-, and sixth-stage adversarial replays found no new
  numerical, exact-link, or coverage gap;
- a separately refereed exact-diagonal sharpening with
  $u_0=1235783/500000$ establishes
  $\mathcal G_2^{(3)}(330867/500000,
  12366348252219/1250000000000)$.  Combined with the pinned Yang--Mao v1
  interfaces and frozen local P6/BookCor retained-spine package, its complete
  wedge proves $C_*\le F_6(1)-3.4754\cdot10^{-6}$ and hence safe base
  `3.780685290`, with certified unrounded upper base
  `3.7806852883796401133524...`.  Two author checks, a genuinely
  non-importing replay, and a different adaptive referee replay use 512-bit
  Arb and reconstruct the exact diagonal, analytic tail, exact budget, P6
  concavity, entire wedge, correlation handoff, and rounding.  The initial
  provenance wording comment was repaired without changing mathematics; the
  resolved referee and secondary result-to-claim gate pass.  All hashes are
  pinned in `routes/upper/PROGRESS.md`.  Safe base `3.780685300` is the
  immediate canonical predecessor; `3.780685320` and `3.780685405` are
  earlier reviewed predecessors.  None of this supplies a finite-$k$ or
  specific finite Ramsey result, global optimum, novelty, priority,
  publication, or world-best claim.

Still required before a community-facing theorem claim:

- external human review of the standalone combinatorial proof, repaired
  BookCor/descent chain, and Yang--Mao strong-separator interface;
- optional formalization beyond the two numerical implementations;
- public archival of the immutable certificate and verification log.

## Lower-bound hard gate

The **paired pathwise companion induction** is proved for fixed `p,C`, and the
red side has now been inserted into the explicit HMS optimized appendix before
coarsening.  This closes a local proof of the public checkpoint

$$
-\tfrac12\log p_C+\tfrac1{20}+\frac{c_R(C)}{4D^2},
\qquad
\frac{c_R(C)}{4D^2}\sim\frac1{32\log C},
$$

for sufficiently large fixed `C`.  The unused `1/120` between HMS's printed
`1/24` and the public `1/20` checkpoint is pre-existing HMS slack; only the
positive `c_R` term is new.  The proof package has passed local source-line,
arithmetic, independent referee replay, and a separate adversarial replay; the
first referee's only formal endpoint comment was repaired. Because the HMS source leaves relevant error constants
in `O/o` notation, the threshold is existential rather than a certified
numerical `C_0`. External human review remains the hard gate before a
community-facing theorem claim.  The analogous blue Hölder split is obstructed
at HMS's minimal `D`; it is not needed because the unchanged blue ledger is not
the bottleneck in this regime.

The history-uniform red-side parameter optimization keeps this same frozen source
ledger but replaces the variance proxy by the exact one-sided CGF, chooses the
largest legal Holder parameter at each group size, and approaches the sharp
limiting cutoff window.  Its explicit term satisfies
$G_*(C)>c_R(C)/(4D^2)$ for every sufficiently large fixed `C`, while
$G_*(C)\sim1/(32\log C)$.  A central-history/Jensen argument shows that this
leading coefficient exhausts only the class of deterministic, per-`k`,
history-uniform centered-moment deficits accumulated additively; it does not
rule out history-dependent averaging, a richer induction state, or changing
the mean, dimension, or blue ledger.  An independent source-level referee
accepted both the strict fixed-`C` theorem and this deliberately narrow method
cap.

The subsequent controlled-residual weighted ledger now supersedes its
exact-merge predecessor.  Relative to the same pinned HMS/Lin--Niu snapshots
and frozen bridge, it preserves $G_*(C)$ and adds
$\widehat H_*(C)\sim1/(64\log C)>0$ for every fixed sufficiently large $C$;
the former $H_*(C)\sim1/(96\log C)$ remains a valid immediate predecessor.
An independent source-level referee verifies the residual-gradient square
completion, exact $K_r$ multiplicities, full cutoff-box Hessian, all centered
costs, non-exchangeable reverse induction, extraction, and rate conversion,
with no double count.  The corrected proof/check/referee hashes are
`2234a769...`, `616040b9...`, and `ef6ddbce...`; the exact-merge predecessor
proof/referee are `29c55690...` and `a8588498...`, and the frozen appendix
bridge is `8d380035...`.  A secondary result-to-claim gate returns `yes/high`.

This remains a source-relative local theorem with an existential,
non-effective $C_0$.  It gives no all-$C$ theorem, finite Ramsey-number
improvement, new blue argument, unconditional source reproof, external human
review, novelty, priority, publication, world-best, or global-optimality
claim.  Its narrow $1/64$ cap is only for the explicitly admissible scalar
class with the same recursion, nonnegative deterministic pre-sampling
increments, uniform scalar full-box square completion, and the same
three-factor Holder/CGF comparison; adaptive and higher-order states are not
covered.

For the Bradač route, ordinary type refinements have a proved
`Theta(q log^2 q)` obstruction.  The literal multi-witness idea and its first
uniform transverse-record repair are now both sharply diagnosed.  There is a
counted family of `Theta_t(q log q)` consecutive unmarked type-zero steps with
geometric branching base at least `(3/16)q^(t+.9(t-1))`; its path surplus over
`(C_tq^t)^k` is `exp(Omega_t(q log^2 q))`.  This refutes the uniform,
all-length `exp(O_t(q log q))` record map, while leaving open sufficiently
large threshold constants.
The follow-up `LATE_TAIL_WEIGHTED_AUDIT.md` proves an `O_t(q^t)` suffix bound
once every type layer is saturated.  It also proves that absolute depth does
not force that state and that any positive scalar local contraction with
recoverable terminal range must pay `exp(Omega_t(q log^2 q))` on the rank-zero
family.  An independent source-level referee accepts these statements.  The
actual `k>=A_t q log q` tuple bound remains open; these are mechanism results,
not a new Ramsey lower bound.

The exact cross-type flow now gives $U_j'=U_j\setminus R_j$: a promoted point leaves
only its old-rank cutoff, not every larger nested cutoff.  Root steps therefore
refute universal common contraction, and a one-step state has many marked
children despite unsaturated `U_0`.  The full family `y -> W(y)` is an exact
Markov state, but the cutoff-size vector is not closed.  The rank-zero family
rules out diversification only through its explicit
`floor((t-1)q log q/12)` prefix; eventual diversification for a larger
constant `A_t` remains open.

## Finite-number hard gate

- Four pinned matrices pass an independently calibrated bitset checker; two
  also pass an independent vertex-selection SAT checker.
- The AlphaEvolve 60-vertex `(3,13)` seed is not one-vertex extendable in two
  solvers.
- Zero through nine new triangles are impossible in the frozen family; ten is
  attainable.
- From the structured 11-triangle near miss, arbitrary additions plus at most
  eight deletions are UNSAT.  The next search must therefore release at least
  nine old edges or leave that basin.
- At budget nine the common triangle edge is forced deleted; the residual
  problem has eight deletions and 25,685 preseeded `I13` hitting clauses.  A
  one-million-conflict interruptible solve was `UNKNOWN`, not UNSAT.
- A Benders master added 24,832 reusable conditional `I13` cuts and 81,345
  exact pairwise shared-deficit incompatibilities.  Its pinned replay also
  stopped at the conflict limit with zero heuristic exclusions and no model;
  it remains `UNKNOWN` and changes no Ramsey bound.
- A higher-order follow-up exhaustively checked 341,376 triples among 128
  high-coverage additions and materialized 57,879 strict ternary
  shared-deficit cuts. Its separately bounded replay also remained `UNKNOWN`;
  no global Ramsey bound changed.
- A deliberately different 61-vertex seed has eleven triangles, no
  independent 13-set, and no edge common to all eleven triangles.  For this
  exact fixed-seed edit family, arbitrary additions plus at most ten input
  deletions are impossible.  Budget ten has three hub-deleted residual-eight
  branches and one CNF merging exactly all 512 hub-retained transversals;
  Glucose and CaDiCaL return UNSAT on all four, every frozen DRAT trace passes
  `drat-trim`, and an independent referee reconstructs the branch projection,
  CNFs, clause banks, and traces.  Therefore any completion in this fixed
  basin needs at least eleven deletions.
- The checked 99-point `(3,18)` certificate has a frozen 100-point extension
  with one triangle and no `I18`.  Three exhaustive triangle-edge branches,
  with arbitrary additions and at most five input-edge deletions, have checked
  DRAT UNSAT proofs.  At exact budget six, all three branches fixing one edge
  of the unique seed triangle absent now have complete, replayed DRAT UNSAT
  proofs.  The newly closed `(97,98)` branch uses a 13,160-variable,
  429,892-clause finite relaxation with 251,771 universal `I18` cuts; its
  6,649,939-line proof received fresh `s VERIFIED` replays in both the author
  record and the independent referee.  Hence this fixed near miss needs at
  least seven input-edge deletions even when original nonedges may be added
  freely.  Neither existence nor nonexistence of an exact-seven repair is
  established, and `R(3,18)>=101` is not proved.  The checker's metadata
  field `seven_deletion_repair_exists: false` is a default value, not evidence
  for the exact-seven question.
- A near miss is never promoted without full reconstruction by the independent
  verifier.
